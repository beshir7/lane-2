// =========================================================================
// Supabase client (placeholder). The datastore currently lives in-memory in
// services/db.ts behind a narrow interface. When we move to Supabase, create
// the client here and have services/db.ts call it — no feature/query/action
// code above the data layer needs to change.
//
//   import { createClient } from "@supabase/supabase-js";
//   export const supabase = createClient(
//     process.env.NEXT_PUBLIC_SUPABASE_URL!,
//     process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
//   );
// =========================================================================

export {};
